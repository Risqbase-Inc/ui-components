# CODEOWNERS — RisqBase UI Components
#
# TODO: Replace `@*-placeholder` handles with real GitHub usernames before
# enabling branch protection's "require review from Code Owners" rule.
# The placeholders below let GitHub parse this file as valid (so the
# workflow scaffolding is reviewable) without silently routing reviews
# to anyone real until the team's GH org handles are confirmed.
#
# Format: <path> <space-separated owner handles>
# Last matching pattern wins; later rules override earlier ones.

# Design tokens (TS source)
src/tokens/**         @alex-chen-placeholder

# Data visualisation primitives
src/data-viz/**       @priya-sharma-placeholder

# AI surface components
src/ai/**             @alex-chen-placeholder

# Documentation site (MDX content + meta)
docs/**               @elena-vasquez-placeholder

# Raw token sources (Style Dictionary input — design + engineering joint owners)
tokens/**             @alex-chen-placeholder @elena-vasquez-placeholder
