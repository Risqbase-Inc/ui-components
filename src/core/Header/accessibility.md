# Header — accessibility

Stub. Header must include skip-link target (§5.8.2), `<header>` landmark, accessible logo (image with text fallback), and visible focus ring on all nav links and CTAs (§5.8.1).

To be fleshed out by Frontend in S2/S3.
