# v4.2.1 — Spec patch backlog

Items deferred from v4.2 that need a spec resolution in v4.2.1. Sourced
from `implementation-plan.md` §8 (Open spec gaps) and `audit.md` flags,
plus reconciliation items recorded as v4.2 ships.

This is a working list — items get appended as they surface, removed
once the v4.2.1 patch lands. Sections below are line-additive: G4,
G1 and G8 each maintain their own subtree; the file composes
cleanly across parallel PRs.

---

## Spec edits made during S1 cleanup

This subsection logs **edits made directly to `spec.md`** during S1
cleanup (rather than deferred to a v4.2.1 patch round). Each entry
names the editor, the date, the PR, the gate, and the rationale, so
v4.2.1 can fold the edits into a single re-publish without
archaeology.

### 2026-05-10 — §14 Dark Mode Pathway + §15.2.1 Theme files + §17 row 43 (G4, G4)

**PR:** `s1/cleanup-3-design` (G1's S1-cleanup-2 holistic finding —
spec contradicts executed plan on dark/HC mode shipping).

**What changed.**

| Section | Before | After |
|---|---|---|
| §14 Dark Mode Pathway | Asserted dark mode ships in v4.3 only; §15.6 tokens "anticipate" dark via the theme dimension but the file does not ship until v4.3. | Asserts dark + HC ship in v4.2: S1 lands the infrastructure (empty stubs, lint, build pipeline override layer); S4 fills values (impl-plan §4 row U5.5, owner G4, reviewer G1). |
| §15.2.1 Theme files table — Dark row | `Stub only (v4.3)` | `Stub in v4.2 S1 (file present, intentionally empty); values land v4.2 S4 (impl-plan §4 row U5.5, owner G4, reviewer G1)` |
| §15.2.1 Theme files table — High-contrast row | `tokens/themes/high-contrast.json` / `Stub only (v4.3)` | `tokens/themes/hc.json` / same phased status as Dark. Filename corrected to match what S1 shipped. |
| §15.2.1 — Default (light) row | `tokens/themes/light.json` | `tokens/themes/light.json *(implicit; primitive/semantic/component layers are the light source)*` — a `themes/light.json` file is **not** present in the S1 layout; the `primitive/`, `semantic/`, `component/` JSON tree IS the light source per `tools/tokens-build/index.js` lines 100–106. |
| §17 verification checklist row 43 | `Three theme files complete (light, print stable; dark, high-contrast stubs)` | `All four theme files present and lint-clean: light + print stable in S1; dark + HC stubs in S1 (empty, lint-validated); dark + HC values land in S4 (impl-plan U5.5)` |

**Why.** D11 in `implementation-plan.md` locks "Modes shipped in v4.2:
Light + dark + HC. No mode deferral." The S4 sprint slot already
exists for the value-fill work (impl-plan §3, S4 owner G4). The
S1 token-build pipeline already emits `[data-theme="dark"]` and
`[data-theme="hc"]` blocks (`tools/tokens-build/index.js` lines
130–138, with stub comment "awaiting S4 values per
implementation-plan §4 (U5.5)"). The pre-edit spec text was the only
remaining contradiction — the rest of the system already executed
against the corrected reading. Per the CEO's "implement the spec as
presented" directive the spec **is** the contract, but here the spec
is out of date relative to a CEO-approved (D11) sprint plan; updating
the spec to match the plan keeps the contract truthful and avoids
rolling back S1's theming infrastructure (which would invalidate
weeks of work and contradict D11).

**Verification.** `npm run lint:tokens` passes against the empty
stubs (lint rule §15.2.1: theme files may only override existing
keys — empty satisfies trivially); `npm run build` regenerates
`dist/tokens.css` with the same stub block S1 ships today.

**v4.2.1 reconciliation.** When the v4.2.1 spec patch publishes,
roll the §14 + §15.2.1 + §17 row 43 edits above into the published
text and remove this entry from the backlog.

---

## Button border-radius — brand-baseline reconciliation

**Status:** Open — needs CEO + G4 adjudication for v4.2.1.

The v4.2 spec does not explicitly mandate a border-radius for
`<Button>`. The closest signals point in two different directions:

1. **`tokens/primitive/dimension.json` line 15** describes the
  `radius.lg` primitive (12px) as `"Large radius — buttons"`. This
  is the canonical statement on button rounding in the v4.2 token
  system. v1.x consumers ship `rounded-xl` (= `radius.lg` = 12px).
  S1's `<Button>` keeps `rounded-xl` and `s1/cleanup-3-design` adds
  the missing `dimension.radius.button.default` role token aliasing
  `{dimension.radius.lg}` so spec §15.4 is honoured.
2. **`GOV-DS-2026-01` brand baseline** specifies `rounded-full` for
  buttons across RisqBase product surfaces (CTAs, primary actions).
  Skip links in `spec.md` line 198 use the "indigo brand pill style"
  in keeping with the brand baseline.

The two anchors disagree. v4.2 spec line 28 ("This is the canonical
world-class baseline. All subsequent revisions are deltas to v4.2.")
makes the spec the system-level contract, but GOV-DS-2026-01
predates v4.2 and is the brand baseline that the marketing site,
RALIA, and Cortex consume directly.

**Decision in `s1/cleanup-3-design`:** keep `rounded-xl` (no pixel
change to v1.x consumers; honour the primitive token's explicit
`"Large radius — buttons"` description). Add the
`dimension.radius.button.default` role token (alias to
`{dimension.radius.lg}`) so `<Button>` consumes a role token per
spec §15.4 rather than a Tailwind utility magic constant.

**v4.2.1 question for the CEO + G4:** is GOV-DS-2026-01's
`rounded-full` baseline still the brand intent for buttons, or has
v4.2's adoption of `rounded-xl` (via the `radius.lg` primitive) made
the pill standard obsolete for buttons (and retained only for
badges and skip links)? Whichever way it lands, the v4.2.1 spec
patch should write it down explicitly in §15.6 (token enumeration)
or §13 (Components retained from v3.1) so future agents do not
re-discover this tension. See
`docs/design-system/v4.2/notes/button-radius-decision.md` for the
S1 rationale in full.

---
# v4.2.1 spec-patch backlog

> Owner: G1 (Architecture) (architecture items); cross-functional owners noted per item.
> Status: Pending — to be addressed in the v4.2.1 patch release after S1 ships.
> Refs: GOV-DS-2026-02 (v4.2 spec), `docs/design-system/v4.2/implementation-plan.md` §8.

Aggregates spec gaps and reconciliation items surfaced during S1
implementation. Each item is a one-line description plus the owner.
Items marked **placeholder** await a separate dispatch from the listed
owner — this file records that the issue is on the radar but is not
the authoring location.

## Checklist

- [ ] **§22.1 separate-repo recommendation → reconcile to "co-locate".**
  We shipped co-located per implementation-plan §2 D1; spec wording must
  track the build. — *Owner: G1 (Architecture)*
- [ ] **§22.5 Astro+MDX stack pick — formally documented.**
  Astro confirmed (implementation-plan §2 D6); §22.5 prose can be
  tightened to record the Next.js variant rejection. — *Owner: G1 (Architecture)*
- [ ] **§15.1 line 1614 references `dist/tokens.ts` which PR #8 removed.**
  Reconcile to the consolidated artefact path
  `dist/tokens/index.{js,mjs,d.ts}` (per PR #8 commit message). —
  *Owner: G1 (Architecture)*
- [ ] **Sonification (audit U1.7).**
  Referenced in `plan.md` but missing from `spec.md`; reconcile by
  adding a §8 paragraph or formally defer to v4.3. — *Owner: G1 (Architecture)
  (reconciliation); PM (defer call)*
- [ ] **Motion tokens.**
  Placeholder pending G4's separate dispatch on enumeration and
  values for `duration.motion.*` and `easing.*`. — *Owner: G4
   (placeholder)*
- [ ] **README owners list rot risk.**
  Placeholder pending G4's separate dispatch on canonical owner
  source vs README + CODEOWNERS drift. — *Owner: G4 (Design)
  (placeholder)*
- [ ] **Audit row count drift across v4.2 docs.**
  Placeholder pending G8's reconciliation pass on plan, audit, and
  spec disagreement. — *Owner: G8 (Growth) (placeholder)*
- [ ] **GOV-DS-2026-02-PLAN-420 and -AUDIT-420 ID resolution.**
  Placeholder pending G8's allocation of these IDs in the
  governance index. — *Owner: G8 (Growth) (placeholder)*
- [ ] **Glossary scope (U3.2).**
  Placeholder pending G8's separate dispatch on the ~80 canonical
  glossary terms authoritative source list. — *Owner: G8 (Growth)
  (placeholder)*
- [ ] **Per-component `tokens.md` content.**
  Placeholder pending Frontend's separate dispatch to populate the
  enumerated role-token list per component to spec §15.4 contract. —
  *Owner: Frontend (reports to G1) (placeholder)*

## Out of scope additions during this cleanup pass

_None at this time. The v8 plugin upgrade originally listed here was
shipped via cleanup-3-arch, which also reverted the
`no-empty-interface` substitute in favour of the originally directed
`no-empty-object-type`._
- [ ] **Bump `@typescript-eslint/eslint-plugin` and
  `@typescript-eslint/parser` v6.21.0 → v8.x to enable
  `no-empty-object-type`.**
  Specified for the rule-tightening pass but unavailable on v6 (added
  in v8.0.0). Substituted `@typescript-eslint/no-empty-interface` as
  the v6-available equivalent in this PR. The v8 plugin upgrade is a
  major dep bump and per OEF v4.2 §4.4 requires G2 + G5
  review and CEO approval — separate PR. — *Owner: G1 (Architecture)
  (proposes); G2 (Security) + G5 (QA) (review); CEO
  (approve)*
# v4.2.1 — Spec patch backlog

Items deferred from v4.2 that need a spec resolution in v4.2.1. Sourced
from `implementation-plan.md` §8 (Open spec gaps) and `audit.md` flags.

This is a working list — items get appended as they surface, removed
once the v4.2.1 patch lands.

---

## Sonification (audit U1.7)

**Status:** Deferred to v4.3 unless a v4.2.1 spec patch reinstates it.

The original `plan.md` U1.7 listed "sonification optional" as part of
chart accessibility, but `spec.md` §8.5 specifies the five mandatory
provisions (per-mark `aria-label`, reduced-motion, forced-colors,
data-table fallback, screen-reader narrative) without sonification —
the sub-item was silently dropped (audit row U1.7 flag). Per the CEO
directive to **implement the spec as presented**, sonification stays
out of v4.2 implementation. If a v4.2.1 spec patch reinstates it (with
a defined audio-cue model, volume control, and
`prefers-reduced-motion`-aware fallback) it ships in v4.3. Otherwise it
remains a v4.3+ candidate. Owner on reinstatement: G4 + Frontend.

---

## Motion tokens enumeration (closed in v4.2)

**Status:** Resolved in `s1/cleanup-2-design` (this PR).

`implementation-plan.md` §8 item 3 listed motion-token enumeration as a
v4.2.1 patch backlog item. Closed early in v4.2 to avoid two rounds of
churn — `tokens/primitive/duration.json` and
`tokens/primitive/easing.json` now ship the four
`duration.motion.{instant,reactive,considered,celebratory}` values
(0ms / 100ms / 300ms / 500ms) and three
`easing.{standard,emphasised,decelerated}` curves referenced (but not
enumerated) in spec §15.3 and §15.5. Spec §15.3 example
`duration.motion.reactive` now resolves; `duration.gauge.reveal` is
preserved as an alias to `motion.celebratory` (both = 500ms). The
v4.2.1 spec patch should retroactively absorb these values into §5
(Motion) and §15.5 ("v4.1 retained verbatim") so the spec text matches
the token catalogue. Verify against AA contrast / reduced-motion
guidance in §8.5.3 — `motion.instant` is the canonical reduced-motion
fallback.
