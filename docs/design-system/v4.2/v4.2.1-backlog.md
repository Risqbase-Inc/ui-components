# v4.2.1 spec-patch backlog

> Owner: Alex Chen (architecture items); cross-functional owners noted per item.
> Status: Pending — to be addressed in the v4.2.1 patch release after S1 ships.
> Refs: GOV-DS-2026-02 (v4.2 spec), `docs/design-system/v4.2/implementation-plan.md` §8.

Aggregates spec gaps and reconciliation items surfaced during S1
implementation. Each item is a one-line description plus the owner.
Items marked **placeholder** await a separate dispatch from the listed
owner — this file records that the issue is on the radar but is not
the authoring location.

## Checklist

- [ ] **§22.1 separate-repo recommendation → reconcile to "co-locate".**
  We shipped co-located per implementation-plan §2 D1; spec wording must
  track the build. — *Owner: Alex Chen*
- [ ] **§22.5 Astro+MDX stack pick — formally documented.**
  Astro confirmed (implementation-plan §2 D6); §22.5 prose can be
  tightened to record the Next.js variant rejection. — *Owner: Alex
  Chen*
- [ ] **§15.1 line 1614 references `dist/tokens.ts` which PR #8 removed.**
  Reconcile to the consolidated artefact path
  `dist/tokens/index.{js,mjs,d.ts}` (per PR #8 commit message). —
  *Owner: Alex Chen*
- [ ] **Sonification (audit U1.7).**
  Referenced in `plan.md` but missing from `spec.md`; reconcile by
  adding a §8 paragraph or formally defer to v4.3. — *Owner: Alex Chen
  (reconciliation); David Chen (defer call)*
- [ ] **Motion tokens.**
  Placeholder pending Elena's separate dispatch on enumeration and
  values for `duration.motion.*` and `easing.*`. — *Owner: Elena
  Vasquez (placeholder)*
- [ ] **README owners list rot risk.**
  Placeholder pending Elena's separate dispatch on canonical owner
  source vs README + CODEOWNERS drift. — *Owner: Elena Vasquez
  (placeholder)*
- [ ] **Audit row count drift across v4.2 docs.**
  Placeholder pending Sophie's reconciliation pass on plan, audit, and
  spec disagreement. — *Owner: Sophie Brennan (placeholder)*
- [ ] **GOV-DS-2026-02-PLAN-420 and -AUDIT-420 ID resolution.**
  Placeholder pending Sophie's allocation of these IDs in the
  governance index. — *Owner: Sophie Brennan (placeholder)*
- [ ] **Glossary scope (U3.2).**
  Placeholder pending Sophie's separate dispatch on the ~80 canonical
  glossary terms authoritative source list. — *Owner: Sophie Brennan
  (placeholder)*
- [ ] **Per-component `tokens.md` content.**
  Placeholder pending Priya's separate dispatch to populate the
  enumerated role-token list per component to spec §15.4 contract. —
  *Owner: Priya Sharma (placeholder)*

## Out of scope additions during this cleanup pass

_None at this time. The v8 plugin upgrade originally listed here was
shipped via cleanup-3-arch, which also reverted the
`no-empty-interface` substitute in favour of the originally directed
`no-empty-object-type`._
- [ ] **Bump `@typescript-eslint/eslint-plugin` and
  `@typescript-eslint/parser` v6.21.0 → v8.x to enable
  `no-empty-object-type`.**
  Specified for the rule-tightening pass but unavailable on v6 (added
  in v8.0.0). Substituted `@typescript-eslint/no-empty-interface` as
  the v6-available equivalent in this PR. The v8 plugin upgrade is a
  major dep bump and per OEF v4.2 §4.4 requires Marcus + Dr. Sarah
  review and Fiyin approval — separate PR. — *Owner: Alex Chen
  (proposes); Marcus Chen + Dr. Sarah Mitchell (review); Fiyin Adeleke
  (approve)*
# v4.2.1 — Spec patch backlog

Items deferred from v4.2 that need a spec resolution in v4.2.1. Sourced
from `implementation-plan.md` §8 (Open spec gaps) and `audit.md` flags.

This is a working list — items get appended as they surface, removed
once the v4.2.1 patch lands.

---

## Sonification (audit U1.7)

**Status:** Deferred to v4.3 unless a v4.2.1 spec patch reinstates it.

The original `plan.md` U1.7 listed "sonification optional" as part of
chart accessibility, but `spec.md` §8.5 specifies the five mandatory
provisions (per-mark `aria-label`, reduced-motion, forced-colors,
data-table fallback, screen-reader narrative) without sonification —
the sub-item was silently dropped (audit row U1.7 flag). Per the CEO
directive to **implement the spec as presented**, sonification stays
out of v4.2 implementation. If a v4.2.1 spec patch reinstates it (with
a defined audio-cue model, volume control, and
`prefers-reduced-motion`-aware fallback) it ships in v4.3. Otherwise it
remains a v4.3+ candidate. Owner on reinstatement: Elena + Priya.

---

## Motion tokens enumeration (closed in v4.2)

**Status:** Resolved in `s1/cleanup-2-design` (this PR).

`implementation-plan.md` §8 item 3 listed motion-token enumeration as a
v4.2.1 patch backlog item. Closed early in v4.2 to avoid two rounds of
churn — `tokens/primitive/duration.json` and
`tokens/primitive/easing.json` now ship the four
`duration.motion.{instant,reactive,considered,celebratory}` values
(0ms / 100ms / 300ms / 500ms) and three
`easing.{standard,emphasised,decelerated}` curves referenced (but not
enumerated) in spec §15.3 and §15.5. Spec §15.3 example
`duration.motion.reactive` now resolves; `duration.gauge.reveal` is
preserved as an alias to `motion.celebratory` (both = 500ms). The
v4.2.1 spec patch should retroactively absorb these values into §5
(Motion) and §15.5 ("v4.1 retained verbatim") so the spec text matches
the token catalogue. Verify against AA contrast / reduced-motion
guidance in §8.5.3 — `motion.instant` is the canonical reduced-motion
fallback.
