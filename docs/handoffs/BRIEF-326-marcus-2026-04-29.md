# BRIEF-326 Handoff — Marcus Chen — 2026-04-29

**Brief**: `Risqbase-Inc/Ralia` → `docs/briefs/BRIEF-326-UI-COMPONENTS-VULN-REMEDIATION-AND-PUBLIC-RESTORE.md`
**Repo**: `Risqbase-Inc/ui-components` (public)
**Branch**: `fix/brief-326-vulns` (off `main` @ `6e701d5`)
**Implementer**: Marcus Chen (G2 — Security Architect)
**Status**: ✅ ready for peer review (G6 Jordan + G1 Alex). G2 self-review forbidden per BRIEF-329 §5b.
**PR opens**: 2026-05-01 (post CI-minute reset)
**Publish**: 2026-05-01 after PR merge

---

## TL;DR for the next agent

`npm audit` previously reported **8 vulnerabilities** (5 high, 3 moderate) covering 9 packages and 18+ GHSA advisories. After this branch, it reports **`found 0 vulnerabilities`**. Build passes, all 12 component exports load, no breaking peer-dep changes, no major-version bumps required. Version bumped 1.2.0 → 1.3.0.

---

## Decisions made

| # | Decision | Rationale | Rejected alternative |
|---|----------|-----------|----------------------|
| 1 | Use `npm overrides` to force `postcss@^8.5.10` rather than `npm audit fix --force` | `--force` would have downgraded `next` to `9.3.3` (massive regression on a package on `16.2.4`). The actual fix is the bundled `postcss` inside `next` and `tsup`; an override resolves both transitively without touching the host package. | `npm audit fix --force` (rejected — destructive); pinning `next@9.3.3` (rejected — would lose 7 years of Next features); waiting for upstream to bump bundled postcss (rejected — uncertain timeline, postcss 8.5.x is wire-compatible with 8.4.x). |
| 2 | Bump devDep `next` from `^16.1.3` to `^16.2.4` | Aligns with RALIA's pinned version per BRIEF-326 §Phase 2 ("RALIA already on Next 16.2.3 — should match or exceed"). 16.2.4 is the latest. | Leave `^16.1.3` (rejected — RALIA's pinned version moved; better to align). |
| 3 | Leave `peerDependencies.next: >=13.0.0` unchanged | Consumer flexibility: RALIA/Cortex/RisqBase can be on any Next ≥13. We only specify the minimum supported, not the latest. The vuln fix is in the consumer's `next` resolution, not ui-components' peer range. | Bump peer to `>=16.0.0` (rejected — would force every consumer onto Next 16+ for no security benefit, since the vuln resolves at the consumer's tree). |
| 4 | Treat `next` major bump as **not required** | Latest available is `16.2.4`, which `npm audit fix` already covers without `--force`. The brief anticipated a major bump might be needed, but the actual state is that all 9 Next.js GHSAs are addressed in the existing 16.2.x patch chain. | Bump to a later major (none exists yet); skip Next bump (rejected — leaves 9 GHSAs open). |
| 5 | Version bump 1.2.0 → 1.3.0 (minor) per brief §Phase 3 | Matches brief language: "vuln fixes are technically bug fixes but include a major Next bump = minor at the package level". Even though the Next bump turned out to be patch-class, an `overrides` block is a meaningful enough config change that minor is appropriate. | Patch (1.2.1) — rejected, brief explicitly specifies minor; major (2.0.0) — rejected, no breaking API changes. |
| 6 | **No `npm publish` in this branch.** Documented for 1 May execution. | CEO directive 2026-04-29 — 0 CI minutes for 48 hours. Publishing now would also pre-empt G6+G1 peer review. | Publish now (rejected — violates CEO directive + bypasses review gate). |
| 7 | **No `gh pr create`.** Branch push only. | Same CI-minute exhaustion directive. PR opens 2026-05-01. | Open PR now (rejected per directive). |

---

## What was done

1. ✅ Branch `fix/brief-326-vulns` created off `main` (`6e701d5`).
2. ✅ `npm install` on clean tree to capture before-state baseline (8 vulns).
3. ✅ `docs/SECURITY-BEFORE-2026-04-29.{txt,json}` captured.
4. ✅ `npm audit fix` ran cleanly (no `--force`) → 6 vulns resolved, 2 outstanding (postcss + next-via-postcss).
5. ✅ `package.json` modified: bumped `next` devDep + added `overrides.postcss: ^8.5.10`.
6. ✅ `npm install` → `found 0 vulnerabilities`.
7. ✅ `npm run build` → tsup emits valid CJS+ESM+DTS.
8. ✅ Smoke check: `node -e "const m = require('./dist/index.js'); console.log(Object.keys(m))"` lists all 12 exports.
9. ✅ `npm version minor --no-git-tag-version` → 1.2.0 → 1.3.0.
10. ✅ `docs/SECURITY-AFTER-2026-04-29.{txt,json}` captured.
11. ✅ Remediation report at `docs/SECURITY-REMEDIATION-2026-04-29.md`.
12. ✅ This handoff at `docs/handoffs/BRIEF-326-marcus-2026-04-29.md`.
13. Pending: commit + push to `origin/fix/brief-326-vulns`.

---

## What was NOT done (intentional, deferred)

- ❌ `npm publish` — deferred to 2026-05-01 after PR review and merge.
- ❌ `gh pr create` — deferred to 2026-05-01 per CEO directive.
- ❌ Consumer-project bumps in RALIA / Cortex / RisqBase — those are separate PRs after the package ships.
- ❌ Dependabot alert verification on the GitHub UI — done after publish (Phase 5 of brief).
- ❌ `gitleaks` history scan (brief §Risks #3) — repo is already public per CEO 2026-04-28, the secrets-in-history check would need to have been done before that flip. If desired, run as a follow-up but not required by this brief.
- ❌ `PACKAGES_TOKEN` revocation review (brief §Phase 5 final bullet) — separate cleanup task; not blocking.

---

## Verification commands for reviewers

```bash
git fetch origin fix/brief-326-vulns
git checkout fix/brief-326-vulns
npm ci                                  # clean install from lockfile
npm audit                               # expect: found 0 vulnerabilities
npm run build                           # expect: green CJS+ESM+DTS
node -e "console.log(Object.keys(require('./dist/index.js')).sort())"
                                        # expect: [Badge, Button, ComingSoonBadge, Footer, GhostButton,
                                        #          Header, MostPopularBadge, NewBadge, PrimaryButton,
                                        #          SecondaryButton, SectionEyebrow, StandaloneProductBadge]
npm ls postcss                          # expect: every entry shows 8.5.12 (overridden)
npm ls next                             # expect: 16.2.4
git diff main -- package.json           # expect: next devDep ^16.1.3 → ^16.2.4, +overrides block, version 1.2.0 → 1.3.0
```

---

## Open questions / known limitations

1. **ESM smoke check via raw `node` fails on `next/link` subpath.** Pre-existing — `next` bundles its subpath exports for use inside a Next bundler, not raw `node`. Both `dist/index.js` (CJS) and `dist/index.mjs` (ESM, when consumed by Next) work correctly. The package has always been published this way. Unaffected by my changes. Consumer `npm install` and Next-app boot is the real smoke test (brief §Phase 4) — that runs as part of consumer-bump PRs on 1 May.

2. **`npm run lint` fails with "No files matching pattern 'src/'".** Pre-existing ESLint v8 + flat-config interaction in the repo (root `.eslintrc.*` likely missing). Unrelated to BRIEF-326. Could be addressed alongside BRIEF-331 (Jordan G6) when the dep-integrity guard re-lands. Not blocking this PR.

3. **Brief vs reality alert count.** Brief lists "20 alerts"; `npm audit` reports 8 packages. The 20 comes from counting GHSAs aggregated across packages (Next.js alone has 9 GHSAs on a single package). Reconciliation: at the package level, 8/8 are fixed; at the GHSA level, all 18+ advisories close.

---

## Required peer reviewers

Per BRIEF-329 §5b (`package.json` + `package-lock.json` changes → G2 + G6, plus G1 for arch on major bumps). I am G2, so G2 self-review forbidden:

- **G6 Jordan** (DevOps) — review `package.json` overrides mechanism, version bump, lockfile integrity, publish workflow assumptions for 1 May.
- **G1 Alex** (Architecture) — review the `next` devDep bump from `^16.1.3` → `^16.2.4`, the postcss override semantics, and the no-major-bump conclusion.

---

## Files changed

```
M  package.json                                     (next devDep, +overrides, version)
M  package-lock.json                                (172 → 173 audited packages, transitive bumps)
A  docs/SECURITY-BEFORE-2026-04-29.txt              (npm audit before)
A  docs/SECURITY-BEFORE-2026-04-29.json
A  docs/SECURITY-AFTER-2026-04-29.txt               (npm audit after)
A  docs/SECURITY-AFTER-2026-04-29.json
A  docs/SECURITY-REMEDIATION-2026-04-29.md          (full remediation report)
A  docs/handoffs/BRIEF-326-marcus-2026-04-29.md     (this file)
```

No `src/` changes. No build-tool changes beyond version bumps. No public API surface changes.

---

## Linkage

- **Unblocks**: BRIEF-331 (Jordan G6 — pre-push dep-integrity guard re-introduction). Resume 3 days after this brief closes per BRIEF-326 §Related.
- **Parent**: BRIEF-324 (surfaced the privacy-switch operational pain).
- **Sister**: BRIEF-324c (Cortex security audit — same W19 security session).
- **Consumer follow-ups**: RALIA/Cortex/RisqBase package bumps to `^1.3.0` post-publish.

---

**End of handoff.** Next agent (or human reviewer): start by running the verification commands above to confirm a clean tree before reviewing the diff.
